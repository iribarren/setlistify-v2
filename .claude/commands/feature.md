Start the new feature workflow for: $ARGUMENTS

Follow the Mandatory Workflow for new features defined in CLAUDE.md:

1. Delegate to the `project-manager-docs` agent to create a feature specification document at `docs/specs/YYYY-MM-DD-feature-name.md` (use today's date, convert the feature name to kebab-case)
2. Present the spec to the user and wait for explicit approval before proceeding
3. After approval, create a `feature/<name>` branch from `master` in the appropriate repo(s)
4. Proceed to implementation with the appropriate agent(s)

Start by launching the project-manager-docs agent now.
